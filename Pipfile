[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Packer only. Not a fourth brain. No runtime dep on TinyToT.

[dev-packages]
pytest = ">=7.0"
pytest-cov = "*"
coverage = "*"
ruff = ">=0.5.6"
build = "*"

[requires]
python_version = "3.9"
